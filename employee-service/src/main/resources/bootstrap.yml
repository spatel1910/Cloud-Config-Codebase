spring:
  cloud:
    config:
      uri: http://localhost:8081
  application:
    name: employee-service

  profiles:
    active: prod
management:
  endpoints:
    web:
      exposure:
        include: "*"
